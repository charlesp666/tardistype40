<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinUiPlayApp.Page2"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUiPlayApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <TextBlock x:Name="pageTitle"
                   Text="Page 2"
                   Margin="16"
                   Style="{StaticResource TitleTextBlockStyle}"
        />

        <HyperlinkButton Content="Click to go to main page"
                         Click="HyperlinkButton_Click"
                         HorizontalAlignment="Center"
        />

        <StackPanel Name="pnlTextRefresh"
                    Margin="100"
        >
            <TextBox Name="UpdatedText" Text="Unchanged"/>
            <TextBlock Text="{Binding CurrentActivityText}" FontSize="20"/>
            <Button Content="Update Text" Click="UpdateTextButton_Click"/>
        </StackPanel>
    </Grid>
</Page>
